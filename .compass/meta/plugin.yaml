plugin:
  name: compass
  version: 0.25.4
  commit: a938a146b0321e95874f65ecefcbf3528acf8a4c
  source: F:/claude/plugins/compass/plugin
  repository: https://github.com/hodor/claude-compass
  installed_at: 2026-09-20
  installed_mode: auto-update
  notes: |
    This vault IS the Compass plugin's own development vault. The plugin source
    at `source` above is what this vault uses to drive its workflow. Editing
    plugin source AND vault state in the same repo is intentional - Compass
    bootstraps itself.
