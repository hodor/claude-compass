---
title: "Handoff: v0.8.1 shipped - sizing, folder-born specs, invisible capture, fleet of 50"
type: handoff
status: active
area: methodology
tags: [handoff, release, invisible-capture, sizing, fleet]
created: 2026-08-24
updated: 2026-08-24
summary: "two initiatives specced, researched, decided, built and distributed in one session; the hot-path tiering (ADR-010) is the natural next plan"
git_branch: master
git_commit: 8ac42ff
---

# Handoff: v0.8.1 shipped

## Start Here
1. [[SPEC-018-scaffolding-invisible-to-the-human]] - the human's two rulings: scaffolding stays hidden; agents run between gates
2. [[ADR-013-detached-worker-quiet-fallback]] - the mechanism, 11 decisions, D-06 corrected and D-11 added at plan review
3. [[PLAN-010-invisible-capture]] - done; wave sections carry the live-fire record and the elaboration deltas
4. [[ADR-010-identity-resident-fetch-mandatory]] - the unbuilt next plan: hot path still over cap (D-09 ordered backfill-then-shrink; backfill is done)
5. `plugin/cli/commands/capture_check.py` - the spawn, the recursion gate, the fallback ladder
6. `bench/scripts/fleet_update.py` - refresh, stamp, merge hook events, doctor, commit, push; dry-run by default

## Session Summary
Two initiatives ran end to end. SPEC-016/ADR-011/PLAN-009 dissolved the spec-sizing judgment after a paired experiment showed agents cannot make it reliably: every spec is born a folder, recursively, and workspaces are declared, undone and self-recorded by command. SPEC-018/ADR-013/PLAN-010 moved lesson capture off the human's screen into a hook-spawned detached worker, verified by two human-approved live experiments and a blind-judged live fire; the cheap tier failed on reasoning, the balanced tier passed and ships as default. v0.8.0 then v0.8.1 went to all 50 vaults, doctor-clean, version stamps repaired.

## Tasks
| Task | Status | Notes |
|------|--------|-------|
| PLAN-009 (12 tasks) | done | strict coverage clean; TASK-090 (blind re-audit denominator) parked until decisions accumulate |
| PLAN-010 (9 tasks) | done | strict coverage clean; TASK-096 POSIX pinned-not-observed (no non-Windows host in the fleet) |
| Fleet v0.8.1 | done | 50/50 doctor-ok; 31 pushed, 2 no remote, 11 no git; 5 push-rejected for the human |
| Hot-path tiering (ADR-010) | not started | index.md + catalog still ~6.6k tokens against the 5k cap; summaries are backfilled so shrinking the index is now lossless |

## Recent Changes
- `plugin/cli/commands/capture_worker.py` - the detached worker: own lock, scoped tools, ledger end rows
- `plugin/cli/commands/capture_check.py:100-125` - quiet channel text; ladder in `_handle_open_opportunity`
- `plugin/cli/commands/sync.py` - aggregate hot-path marker; duplicate catalog rows collapse and are reported
- `plugin/cli/vaultlib.py:read_vault_text` - BOM stripped on every vault read (issue #8's real cause)
- `plugin/cli/commands/validate.py` - `summary` expected on every type; sizing-log reconciliation warnings
- `plugin/skills/lesson-write/SKILL.md:35,81,97,123` - skill text in the anti-list; over-cap resolves to refinement; never insert rows; `source` persisted
- `plugin/templates/rules/compass-pipeline.md` - "between gates, silence"; the index-is-an-index invariant
- `plugin/hooks/hooks.json` - SessionStart entry (capture-check) for stranded opportunities

## Decisions
- SPEC-016 D-06/D-07 (human): every spec born folder-like, recursively - the sizing judgment dissolves rather than improves
- SPEC-018 D-01/D-02 (human): scaffolding hidden, never removed; agents run between gates
- ADR-013 D-09 amended: capture worker ships on the balanced tier; cheap failed the blind judge on two runs
- lesson-write over-cap branch: applied on the orchestrator's recommendation after the human said "do them all"; revisit if the human disagrees

## Learnings
- The auto-mode classifier refuses to let the model arm its own instruction channel even with relayed approval; the human running the patch script is the clean path
- `Path.read_text` already normalizes CRLF; the catalog corruption was a BOM, and a fix that passed its own tests fixed nothing until reverted to prove it
- Two blind reviewers disagreed on one archived lesson (anti-list vs dedup tiebreak); the contract, not the reviewers, was amended

## Action Items
1. [ ] Human: the 5 push-rejected projects - ayon-maya-toolkit and ue5-editor-mcp behind their remotes; pg-jira-exporter's remote gone; two with no upstream (`push -u` fixes)
2. [ ] Human: restart open sessions to load the SessionStart hook and the new rule
3. [ ] Next plan: hot-path tiering per [[ADR-010-identity-resident-fetch-mandatory]] - identity-only index, lessons fetch from the harness, `lesson-coverage` as a gate, `admit-check`/`touched` wired or retired
4. [ ] Watch the ledger: `compass doctor`'s worker-ledger row and `compass sizing stats` are the falsification instruments; a zero miss or correction rate is a finding, not health

## Context for Resuming
- Every lesson pass now runs invisibly; the human sees nothing unless a worker fails twice. If a "Stop hook feedback" line ever appears, that is the block last resort firing - check the ledger for why the quiet channel did not suffice.
- The paired-judge criterion has a known defect recorded in PLAN-010's live-fire section: limb (i) is symmetric; score limbs against the contract, not the other pass.
- `.compass/tmp/` holds the phase reports, worker logs, fleet logs and the sizing-prototype corpus (15 items, the fleet's entire non-flat population); none are committed by repo rule.
